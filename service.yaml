name: stencil
versions:
  go: 1.17

# Support bootstrap
library: true
commands:
  - stencil
reportingTeam: cia-dev-tooling
releaseOptions:
  allowMajorVersions: true

# Support newer stencil
arguments:
  library: true
  commands:
    - stencil
  reportingTeam: cia-dev-tooling
  releaseOptions:
    allowMajorVersions: true
